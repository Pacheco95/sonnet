# Overlay ports

Patched copies of vcpkg ports, picked up through `vcpkg-configuration.json` ahead of the registry ([ADR-0004](../docs/decisions/0004-vcpkg-first.md)). Each directory says what was changed and why, so the patch can be dropped once upstream carries it.

| Port | Change |
|---|---|
| `imgui` | Three changes, copied from the registry at baseline `a1cae005` and bumped to port-version 3. The `sdl3-binding`, `sdl3-renderer-binding` and `sdlgpu3-binding` features depend on `sdl3` with its default features off: the registry port depends on `sdl3` with defaults on, which on Linux re-enables `ibus`, and through it `dbus[systemd]` and `libsystemd`, for every consumer, even one that disabled them at the top level. The `vulkan-binding` feature depends on `vulkan-headers` only and compiles the backend with `IMGUI_IMPL_VULKAN_NO_PROTOTYPES`: the registry port links the `vulkan-loader` port, which put a second loader into the process ahead of the one SDL loads, and vcpkg's loader is built without X11 or Wayland surface support, so the editor could not open a window outside an environment that put the SDK's loader first ([ADR-0006](../docs/decisions/0006-vulkan-object-ownership.md)). `viewport-images-start-undefined.patch` removes the Vulkan backend's creation-time transition of a secondary viewport's swapchain images to `PRESENT_SRC_KHR`, submitted before any image is acquired, which the validation layer reports as `UNASSIGNED-non-acquired-swapchain-image-used` every time such a window is resized; the per-frame barrier now starts from `UNDEFINED`, which every viewport frame allows since it clears or discards the image. |
| `sdl3` | The X11 extensions an editor needs are enabled: XInput2 (relative mouse mode and raw motion), Xcursor, Xfixes, XRandR and XScrnSaver. The registry port turns every X11 extension off so host libraries cannot change SDL's capabilities, which leaves `SDL_SetWindowRelativeMouseMode` unsupported on X11. SDL loads these libraries at run time; the build needs their headers (`libxi-dev`, `libxcursor-dev`, `libxrandr-dev`, `libxss-dev` on Debian and Ubuntu) and stops with a message naming the missing one. The CI runners install them. Copied from the registry at baseline `a1cae005` and bumped one port-version. |
| `nlohmann-json` | `byte-swap-unreachable-code.patch` chains `binary_reader::byte_swap`'s `if constexpr` branches with `else`. Where the standard library has `std::byteswap`, the byte loop after them stays in every instantiation that returns early, and MSVC reports it as unreachable code (C4702) while generating code for the caller's translation unit, where `/external:W0` does not reach, so `Bundle.cpp`'s CBOR read failed the Windows build under `/WX`. Copied from the registry at baseline `a1cae005` and bumped one port-version. |
| `ktx` | KTX-Software decides its Clang-only floating-point flag (`-ffp-model=precise`) from the C++ compiler id and applies it to C sources too, so a GNU-style C compiler paired with Clang for C++ (`cc` from GCC next to a locally built `c++` from LLVM) fails on every C file. `0007-clang-flags-per-language.patch` decides per language with `COMPILE_LANG_AND_ID`. `ktxTexture2_constructFromStream` leaks the stream it was handed (a heap allocation, for a memory stream) when the initial header read fails, because that only happens before `ktxTexture_constructFromStream` copies the stream into the texture and takes ownership of it; reading a file too short to hold a KTX2 header leaked 40 bytes under LeakSanitizer. `0008-fix-stream-leak-on-short-read.patch` destructs the stream on that early return. The vendored Basis Universal's `job_pool`, which `ktxTexture2_CompressBasisEx` creates and destroys around every compression, set its kill flag without holding its mutex before `notify_all`: a worker that had just read the flag as false in its wait's predicate, and had not yet blocked, missed the notification, slept for ever, and the destructor's `join` with it. On two cores that hung within a few thousand compressions, and it is what hung `assets_tests` on CI ([issue #24](https://github.com/Pacheco95/sonnet/issues/24)). `0009-job-pool-kill-flag-under-mutex.patch` sets the flag under the mutex, as upstream Basis Universal now does. KTX-Software's `main` carries the fixed Basis Universal, but its latest release, 4.4.2, does not; when the registry's `ktx` moves past 4.4.2, drop the patch and let `compressing KTX2 textures back to back never hangs basisu's job pool` confirm the release has the fix. Copied from the registry at baseline `a1cae005` and bumped one port-version. |
| `lua` | On Windows with dynamic linkage the C++ build (`lua-cpp`) never exported the `luaopen_*` functions: `luaconf.h` defines `LUAMOD_API` as plain `extern "C"` for C++, without the `dllexport` that `LUA_API` carries, so sol2's `open_libraries` failed to link in every binary using scripting. The portfile adds `LUA_API` to that definition on Windows dynamic builds only. Copied from the registry at baseline `a1cae005` and bumped one port-version. |
